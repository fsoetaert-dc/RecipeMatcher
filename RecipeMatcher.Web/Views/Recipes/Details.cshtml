@model Recipe


<div class="text-center">
    <h1>@Model.Name</h1>
    <li>Preparation time: @Model.PreparationMinutes minutes</li>
    @foreach (RecipeIngredient recipeIngredient in Model.RecipeIngredients)
    {
        <li>@recipeIngredient.Ingredient.Name</li>
    }

</div>
