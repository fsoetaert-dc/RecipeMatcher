@model Recipe

<form asp-action="Delete" asp-route-id="@Model.Id" method="post">
    <p>Are you sure you want to delete this recipe? @Model.Name</p>

    <button type="submit">Yes</button>
    <button type="button" onclick="location.href='@Url.Action("Index", "Recipes")'">No</button>
</form>